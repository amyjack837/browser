services:
  - type: web
    name: telegram-media-bot
    env: node
    plan: starter
    buildCommand: "npm install"
    startCommand: "npm start"
    envVars:
      - key: BOT_TOKEN
        sync: false
    instanceCount: 1
    instanceType: starter
